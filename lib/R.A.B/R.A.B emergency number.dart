import 'package:flutter/material.dart';

class RAB_EmergencyNumber extends StatelessWidget {
  const RAB_EmergencyNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("R.A.B"),
      ),
      body: Center(
        child: Text("R.A.B"),
      ),
    );
  }
}
