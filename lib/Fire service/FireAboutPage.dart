import 'package:flutter/material.dart';

class FireAboutPage extends StatelessWidget {
  const FireAboutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
