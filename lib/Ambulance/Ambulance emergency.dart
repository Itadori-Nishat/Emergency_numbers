import 'package:flutter/material.dart';

class AmbulanceEmergencyNumber extends StatelessWidget {
  const AmbulanceEmergencyNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Ambulance"),
      ),
      body: Center(
        child: Text("Ambulance"),
      ),
    );
  }
}
